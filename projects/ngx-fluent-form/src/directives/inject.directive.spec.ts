import { FluentInjectDirective } from './inject.directive';

describe('FluentInjectDirective', () => {
  it('should create an instance', () => {
    const directive = new FluentInjectDirective();
    expect(directive).toBeTruthy();
  });
});
