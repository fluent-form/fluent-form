import { FluentTemplateContextTypeDirective } from './template-context-type.directive';

describe('FluentTemplateContextTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new FluentTemplateContextTypeDirective();
    expect(directive).toBeTruthy();
  });
});
