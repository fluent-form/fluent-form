import { FluentFormControlNamePipe } from './fluent-form.pipe';

describe('FluentFormControlNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FluentFormControlNamePipe();
    expect(pipe).toBeTruthy();
  });
});
