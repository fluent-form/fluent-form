import { FluentWidgetTemplateRefPipe } from './widget-template-ref.pipe';

describe('FluentWidgetTemplateRefPipe', () => {
  it('create an instance', () => {
    const pipe = new FluentWidgetTemplateRefPipe();
    expect(pipe).toBeTruthy();
  });
});
