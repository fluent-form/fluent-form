import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { meta, story, source } from './';

<Meta
  title="FluentForm/Advanced"
  component={meta.component}
  decorators={meta.decorators}
  argTypes={meta.argTypes}
  parameters={meta.parameters} />

# Custom Layout - 自定义布局（Experimental）

如果需要自定义表单布局，`ngx-fluent-form` 提供了一组指令，其用法接近于 [Angular 响应式表单指令](https://angular.cn/guide/reactive-forms)，开发者可以借助这些指令创建自定义布局：

<Canvas withToolbar isExpanded mdxSource={source}>
  <Story name="Custom Layout">{story}</Story>
</Canvas>
