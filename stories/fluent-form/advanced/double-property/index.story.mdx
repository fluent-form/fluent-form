import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { meta } from '../../';
import { story, source } from './';

<Meta
  title="FluentForm/Advanced"
  component={meta.component}
  decorators={meta.decorators}
  argTypes={meta.argTypes}
  parameters={meta.parameters} />

# Double Property - 双字段映射

对于区间选择控件，例如 `range` 控件，它将输出一个包含**两个元素**的元组，你可能期望将元组里的这**两个元素**分别映射到模型里的**两个属性**：

<Canvas withToolbar isExpanded mdxSource={source}>
  <Story name="Double Property">{story}</Story>
</Canvas>
