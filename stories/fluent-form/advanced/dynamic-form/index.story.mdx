import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { meta, story, source } from './';

<Meta
  title="FluentForm/Advanced"
  component={meta.component}
  decorators={meta.decorators}
  argTypes={meta.argTypes}
  parameters={meta.parameters} />

# Dynamic Form - 动态表单

对于需要动态添加/移除组件的表单，可以通过手动更新 `schema` 参数来实现。

<Canvas withToolbar isExpanded mdxSource={source}>
  <Story name="Dynamic Form">{story}</Story>
</Canvas>
