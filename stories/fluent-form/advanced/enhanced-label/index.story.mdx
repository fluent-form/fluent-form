import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { meta } from '../../';
import { story, source } from './';

<Meta
  title="FluentForm/Advanced"
  component={meta.component}
  decorators={meta.decorators}
  argTypes={meta.argTypes}
  parameters={meta.parameters} />

# Enhanced Label - 增强标签

`label` 选项除了可以传入字符串，还可以传入一个配置对象以完全配置标签。

主要功能：
- 设置标签宽度，实现标签右对齐。
- 设置标签提示

<Canvas withToolbar mdxSource={source}>
  <Story name="Enhanced Label">{story}</Story>
</Canvas>
