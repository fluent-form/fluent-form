import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { meta, story, source } from './';

<Meta
  title="FluentForm/Advanced"
  component={meta.component}
  decorators={meta.decorators}
  argTypes={meta.argTypes}
  parameters={meta.parameters} />

# Form Linkage - 表单联动

- 对于表单控件之间存在联动的，通常可以通过手动更新 `schemas` 参数来实现。
- 对于表单控件之间的联动隐藏，可以使用 `hidden` 选项，它支持传入一个函数，函数入参为当前模型值。

<Canvas withToolbar mdxSource={source}>
  <Story name="Form Linkage">{story}</Story>
</Canvas>
