import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { meta, story, source } from './';

<Meta
  title="FluentForm/Advanced"
  component={meta.component}
  decorators={meta.decorators}
  argTypes={meta.argTypes}
  parameters={meta.parameters} />

# Form in Table - 表格中的表单（Experimental）

鉴于表格的复杂性与灵活性，`ngx-fluent-form` 并没有对表格进行封装，而是提供了一组指令供开发者自定义视图：

<Canvas withToolbar mdxSource={source}>
  <Story name="Form in Table">{story}</Story>
</Canvas>
