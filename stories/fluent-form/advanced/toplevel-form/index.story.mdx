import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { meta } from '../../';
import { story, source } from './';

<Meta
  title="FluentForm/Advanced"
  component={meta.component}
  decorators={meta.decorators}
  argTypes={meta.argTypes}
  parameters={meta.parameters} />

# Toplevel Form - 顶层表单

如果想配置顶层表单，`form()` 还支持传入第二个参数，用来配置顶层表单的验证器（`validators / asyncValidators`）与更新时机（`updateOn`）。

<Canvas withToolbar isExpanded mdxSource={source}>
  <Story name="Toplevel Form">{story}</Story>
</Canvas>
