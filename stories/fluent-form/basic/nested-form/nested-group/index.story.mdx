import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { meta } from '../../../';
import { story, source } from './';
import { FluentFormComponent } from 'ngx-fluent-form';

<Meta
  title="FluentForm/Basic/Nested Form"
  component={meta.component}
  decorators={meta.decorators}
  argTypes={meta.argTypes}
  parameters={meta.parameters} />

# Nested Form Group - 嵌套表单组

要构建复杂的表单，推荐使用 [FormGroup](https://angular.cn/api/forms/FormGroup) 把大型表单组织成一些稍小的、易管理的分组。
可以使用 `group` 控件来配置嵌套的表单组（嵌套对象），支持多级混合嵌套：

<Canvas withToolbar isExpanded mdxSource={source}>
  <Story name="Nested Group">{story}</Story>
</Canvas>

<ArgsTable of={FluentFormComponent} />
